/**
 * Module B - Standalone utilities
 */

export function helperFunction(input: string): string {
  return input.toUpperCase();
}

export function anotherHelper(value: number): number {
  return value * 2;
}



























